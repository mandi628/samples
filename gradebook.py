#! /usr/bin/env python

grades = {"":[]}

def add_grade():
    name = input("Enter student name: ")
    grade = int(input("Enter grade to add: "))
    # Lookup name amd add grade to list

def add_student():
    # Ask for student name
    # Add student to dictionary with empty list
